{
  "schemaVersion": 1,
  "invocationTemplates": [
    {
      "id": "en_real_send",
      "language": "en-US",
      "title": "Search a user and send an X direct message",
      "template": "Use BitBrowser profile {{profileName}} to open X Chat, click New message, type @{{recipientHandle}} into the visible search box, click the exact user result, and send this message: {{message}}. Do not construct a conversation URL. Turn off Safety Rehearsal for real sending and keep the profile open when finished.",
      "requiredFields": [
        "profileName",
        "recipientHandle",
        "message"
      ]
    },
    {
      "id": "zh_real_send",
      "language": "zh-CN",
      "title": "搜索用户并发送 X 私信",
      "template": "请使用 BitBrowser profile {{profileName}} 打开 X Chat，点击新消息，在可见搜索框逐字输入 @{{recipientHandle}}，点击准确用户结果并发送以下私信：{{message}}。不要拼接会话 URL。关闭安全演练后真实发送，任务结束保持浏览器打开。",
      "requiredFields": [
        "profileName",
        "recipientHandle",
        "message"
      ]
    },
    {
      "id": "en_preview",
      "language": "en-US",
      "title": "Preview X direct-message outreach",
      "template": "Use BitBrowser profile {{profileName}} to open X Chat, click New message, search @{{recipientHandle}}, click the exact result, and prepare this message: {{message}}. Turn on Safety Rehearsal so the message is typed and captured but not sent. Keep the profile open when finished.",
      "requiredFields": [
        "profileName",
        "recipientHandle",
        "message"
      ]
    },
    {
      "id": "zh_preview",
      "language": "zh-CN",
      "title": "预演 X 私信触达",
      "template": "请使用 BitBrowser profile {{profileName}} 打开 X Chat，点击新消息，搜索 @{{recipientHandle}} 并点击准确结果，准备以下私信：{{message}}。打开安全演练，只输入并截图，不点击发送，任务结束保持浏览器打开。",
      "requiredFields": [
        "profileName",
        "recipientHandle",
        "message"
      ]
    }
  ]
}
{
  "schemaVersion": 1,
  "invocationTemplates": [
    {
      "id": "zh_complete",
      "language": "zh-CN",
      "title": "搜索用户并发送 X 私信",
      "template": "请使用 BitBrowser profile“{{profileName}}”打开 X Chat，点击新消息，在搜索框逐字输入 @{{recipientHandle}}，点击准确用户结果并发送以下私信：{{message}}。不得拼接会话 URL。关闭安全演练后真实发送，任务结束保持浏览器打开。",
      "requiredFields": ["profileName", "recipientHandle", "message"]
    },
    {
      "id": "en_complete",
      "language": "en-US",
      "title": "Search a user and send X DM outreach",
      "template": "Use BitBrowser profile {{profileName}} to open X Chat, click New message, type @{{recipientHandle}} into the visible search box, click the exact user result, and send this message: {{message}}. Do not construct a conversation URL. Turn off Safety Rehearsal for real sending and keep the profile open when finished.",
      "requiredFields": ["profileName", "recipientHandle", "message"]
    },
    {
      "id": "zh_dry_run",
      "language": "zh-CN",
      "title": "演练 X 私信触达",
      "template": "请使用 BitBrowser profile“{{profileName}}”打开 X Chat，点击新消息，搜索 @{{recipientHandle}} 并点击准确结果，准备以下私信：{{message}}。打开安全演练，只输入并截图，不点击发送，任务结束保持浏览器打开。",
      "requiredFields": ["profileName", "recipientHandle", "message"]
    },
    {
      "id": "en_dry_run",
      "language": "en-US",
      "title": "Preview X DM outreach",
      "template": "Use BitBrowser profile {{profileName}} to open X Chat, click New message, search @{{recipientHandle}}, click the exact result, and prepare this message: {{message}}. Turn on Safety Rehearsal so the message is typed and captured but not sent, and keep the profile open when finished.",
      "requiredFields": ["profileName", "recipientHandle", "message"]
    }
  ]
}
